import HomePage from '../pages/HomePage';
import CartPage from '../pages/CartPage';

const routes: any[] = [
    {
        element: HomePage,
        path: '/'
    },
    {
        element: CartPage,
        path: '/cart'
    }
];

export default routes;