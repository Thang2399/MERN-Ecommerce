export type supportsLanguagesArrTypes = {
    code: string,
    language: string,
    flag: string
};

export type paginationDataType = {
    total: number,
    currentPage: number,
    pageSize: number,
}
