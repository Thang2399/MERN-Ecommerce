export type supportsLanguagesArrTypes = {
    code: string,
    language: string,
    flag: string
};

export type singleItemTypes = {
    _id: string,
    name: string,
    description: string,
    currency: string,
    price: string,
    isPromoted: boolean,
    promotion: string,
    imageUrl: string,
    discountPercentage: string,
    brand: string,
    category: string,
    quantity: number
}
